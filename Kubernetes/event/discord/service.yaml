apiVersion: v1
kind: Service
metadata:
  name: event-discord
  labels:
    organization: saveapis
    project: streamer.event-cloud
    app: event-discord
spec:
  selector:
    organization: saveapis
    project: streamer.event-cloud
    app: event-discord
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: NodePort
