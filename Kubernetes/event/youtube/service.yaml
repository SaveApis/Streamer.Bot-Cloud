apiVersion: v1
kind: Service
metadata:
  name: event-youtube
  labels:
    organization: saveapis
    project: streamer.event-cloud
    app: event-youtube
spec:
  selector:
    organization: saveapis
    project: streamer.event-cloud
    app: event-youtube
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: NodePort
